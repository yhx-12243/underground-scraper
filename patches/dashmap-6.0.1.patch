diff --git a/src/mapref/entry.rs b/src/mapref/entry.rs
index f4cc681..6fa4a39 100644
--- a/src/mapref/entry.rs
+++ b/src/mapref/entry.rs
@@ -216,6 +216,13 @@ impl<'a, K: Eq + Hash, V> OccupiedEntry<'a, K, V> {
         self.key
     }
 
+    pub fn into_key_ref(self) -> (K, RefMut<'a, K, V>) {
+        unsafe {
+            let (k, v) = self.bucket.as_ref();
+            (self.key, RefMut::new(self.shard, k, v.as_ptr()))
+        }
+    }
+
     pub fn key(&self) -> &K {
         unsafe { &self.bucket.as_ref().0 }
     }
diff --git a/src/set.rs b/src/set.rs
index 1411008..6e29a6c 100644
--- a/src/set.rs
+++ b/src/set.rs
@@ -19,7 +19,7 @@ use std::collections::hash_map::RandomState;
 ///
 /// [`DashMap`]: struct.DashMap.html
 pub struct DashSet<K, S = RandomState> {
-    pub(crate) inner: DashMap<K, (), S>,
+    pub inner: DashMap<K, (), S>,
 }
 
 impl<K: Eq + Hash + fmt::Debug, S: BuildHasher + Clone> fmt::Debug for DashSet<K, S> {
