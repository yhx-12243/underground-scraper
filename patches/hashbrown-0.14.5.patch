diff --git a/src/map.rs b/src/map.rs
index 88a8265..e0bfccb 100644
--- a/src/map.rs
+++ b/src/map.rs
@@ -5265,6 +5265,11 @@ impl<'a, K, V: Default, S, A: Allocator> Entry<'a, K, V, S, A> {
 }
 
 impl<'a, K, V, S, A: Allocator> OccupiedEntry<'a, K, V, S, A> {
+    #[cfg_attr(feature = "inline-more", inline)]
+    pub fn into_key(self) -> Option<K> {
+        self.key
+    }
+
     /// Gets a reference to the key in the entry.
     ///
     /// # Examples
diff --git a/src/set.rs b/src/set.rs
index 2125a7a..935972f 100644
--- a/src/set.rs
+++ b/src/set.rs
@@ -7,7 +7,7 @@ use core::hash::{BuildHasher, Hash};
 use core::iter::{Chain, FusedIterator};
 use core::ops::{BitAnd, BitOr, BitXor, Sub};
 
-use super::map::{self, DefaultHashBuilder, HashMap, Keys};
+use super::map::{self, DefaultHashBuilder, HashMap, Keys, RawEntryBuilder, RawEntryBuilderMut};
 use crate::raw::{Allocator, Global, RawExtractIf};
 
 // Future Optimization (FIXME!)
@@ -1226,6 +1226,16 @@ where
 }
 
 impl<T, S, A: Allocator> HashSet<T, S, A> {
+    #[cfg_attr(feature = "inline-more", inline)]
+    pub fn raw_entry_mut(&mut self) -> RawEntryBuilderMut<'_, T, (), S, A> {
+        self.map.raw_entry_mut()
+    }
+
+    #[cfg_attr(feature = "inline-more", inline)]
+    pub fn raw_entry(&self) -> RawEntryBuilder<'_, T, (), S, A> {
+        self.map.raw_entry()
+    }
+
     /// Returns a reference to the [`RawTable`] used underneath [`HashSet`].
     /// This function is only available if the `raw` feature of the crate is enabled.
     ///
@@ -2304,6 +2314,11 @@ impl<'a, T, S, A: Allocator> Entry<'a, T, S, A> {
 }
 
 impl<T, S, A: Allocator> OccupiedEntry<'_, T, S, A> {
+    #[cfg_attr(feature = "inline-more", inline)]
+    pub fn into_key(self) -> Option<T> {
+        self.inner.into_key()
+    }
+
     /// Gets a reference to the value in the entry.
     ///
     /// # Examples
